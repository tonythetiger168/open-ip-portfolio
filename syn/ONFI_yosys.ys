# ONFI synthesis script -- Yosys (open-source flow)
read_verilog -sv ../rtl/ONFI_top.sv 
hierarchy -check -top ONFI_top
proc; opt; fsm; opt; memory; opt
techmap; opt
stat
write_json ONFI_top.json
write_verilog ONFI_top_netlist.v
