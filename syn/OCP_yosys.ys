# OCP synthesis script -- Yosys (open-source flow)
read_verilog -sv ../rtl/OCP_top.sv 
hierarchy -check -top OCP_top
proc; opt; fsm; opt; memory; opt
techmap; opt
stat
write_json OCP_top.json
write_verilog OCP_top_netlist.v
