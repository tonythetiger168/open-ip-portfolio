# CAN synthesis script -- Yosys (open-source flow)
read_verilog -sv ../rtl/CAN_top.sv 
hierarchy -check -top CAN_top
proc; opt; fsm; opt; memory; opt
techmap; opt
stat
write_json CAN_top.json
write_verilog CAN_top_netlist.v
