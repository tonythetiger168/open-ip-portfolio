# PWM synthesis script -- Yosys (open-source flow)
read_verilog -sv ../rtl/PWM_top.sv 
hierarchy -check -top PWM_top
proc; opt; fsm; opt; memory; opt
techmap; opt
stat
write_json PWM_top.json
write_verilog PWM_top_netlist.v
