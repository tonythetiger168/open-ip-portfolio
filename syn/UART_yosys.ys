# UART synthesis script -- Yosys (open-source flow)
read_verilog -sv ../rtl/UART_top.sv 
hierarchy -check -top UART_top
proc; opt; fsm; opt; memory; opt
techmap; opt
stat
write_json UART_top.json
write_verilog UART_top_netlist.v
